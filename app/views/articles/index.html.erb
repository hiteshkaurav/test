<h1>Listing articles</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th style="padding: 5px">Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td style="padding: 5px"><%= article.title %></td>
        <td style="padding: 5px"><%= article.body %></td>
        <td style="padding: 5px"><%= link_to 'Show', article %></td>
        <td style="padding: 5px"><%= link_to 'Edit', edit_article_path(article) %></td>
        <td style="padding: 5px"><%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article', new_article_path %>
